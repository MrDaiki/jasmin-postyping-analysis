
export fn test() -> reg u64 {
    reg u64 y;
    reg u64 x;
    x=1;
    y=1;

    if (y<1) {
        y=2;
    }

    else {
        y=3;
    }

    while (y<1) {
        x+=1;
    }

    return x;

}